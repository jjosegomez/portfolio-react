import React from "react";
import { useState } from "react";

const SkillItem = (props) => {
    return(
            <img className="skill-item" src={props.img}></img>
    )
}

export default SkillItem